[Unit]
Description=Harbor
After=docker.service network.target

[Service]
Type=simple
ExecStart=/bin/bash -c "sleep 30 && cd /opt/harbor/ && docker-compose down &>/dev/null && docker-compose up -d"
User=root

[Install]
WantedBy=multi-user.target